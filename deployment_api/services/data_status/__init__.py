"""Decomposed implementation package for ``DataStatusService``.

Modules here are implementation detail — import the service (and the
legacy module-level helper names) from
``deployment_api.services.data_status_service`` (the facade). The
mixins form a linear inheritance chain; see each module docstring.
"""
