name: Quality Gates

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  quality-gates:
    uses: IggyIkenna/unified-trading-pm/.github/workflows/python-quality-gates.yml@live-defi-rollout
    with:
      dep_repos: "unified-cloud-interface unified-events-interface unified-events-interface unified-trading-library unified-cloud-interface unified-config-interface"
    secrets:
      GH_PAT: ${{ secrets.GH_PAT }}
